node_modules/
dist/
.env
.env.*
*.local
!.env.example
deploy.zip
# Kudu log-stream capture, used by the remote UAT run (DEPLOY.md section 6).
stream.log
demo-shots/

# The PlantUML renderer, fetched by tools/render.*; 22 MB of binary that every machine can
# re-download. See lab2/README.md for the version pin.
tools/plantuml.jar
# A Cygwin crash dump. Never anything but noise.
bash.exe.stackdump

# Screen photographs from src/tools/screen-shots.ts. Regenerated on demand; they are
# evidence for a review, not source, and they are several megabytes of PNG per run.
screen-shots/
screen-shots-after/
demo-shots/
